<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="14dp"
    android:background="@drawable/widget_bg">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_description"
        android:textSize="12sp"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp"
        android:fontFamily="sans-serif-medium" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageButton
            android:id="@+id/widget_down"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_widget_down"
            android:background="@drawable/widget_btn"
            android:scaleType="center"
            android:contentDescription="@string/widget_vol_down" />

        <View
            android:layout_width="10dp"
            android:layout_height="1dp" />

        <ImageButton
            android:id="@+id/widget_toggle"
            android:layout_width="56dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_widget_toggle"
            android:background="@drawable/widget_btn_primary"
            android:scaleType="center"
            android:contentDescription="@string/widget_mute" />

        <View
            android:layout_width="10dp"
            android:layout_height="1dp" />

        <ImageButton
            android:id="@+id/widget_up"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_widget_up"
            android:background="@drawable/widget_btn"
            android:scaleType="center"
            android:contentDescription="@string/widget_vol_up" />

    </LinearLayout>
</LinearLayout>
